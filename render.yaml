services:
  - type: web
    name: tarot-api
    runtime: node
    buildCommand: npm install
    startCommand: npx tsx src/server.ts
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
